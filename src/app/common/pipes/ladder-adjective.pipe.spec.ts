import { LadderAdjectivePipe } from './ladder-adjective.pipe';

describe('LadderAdjectivePipe', () => {
  it('create an instance', () => {
    const pipe = new LadderAdjectivePipe();
    expect(pipe).toBeTruthy();
  });
});
